tosca_definitions_version: tosca_2_0

metadata:
  puccini.scriptlet:tosca.validation.throw_string: |
    exports.validate = function(v) {
      throw "I am a string, not an error object";
    };

node_types:
  MyType:
    properties:
      my_prop:
        type: string
        validation: { $throw_string: [] }

service_template:
  node_templates:
    my_node:
      type: MyType
      properties:
        my_prop: "some value"
